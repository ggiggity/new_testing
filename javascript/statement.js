var x,y,z; //statement1
x = 5;      //statement2
y = 6;      //statement3
z = x + y;  //statement4

//statement = values, operators, expressions, keywords and comments.

//loop

function long(r1 = 12, r2 = 15) {
    return r1 * r2
}

var cars = ["doyoda", "volvo", "sob", "rob", "dob"]
var i;
var text = ""
for (i = 0; i < 12; i++) {
    text += cars[i]
}